#include "Router.hpp"

Router::Router() {}



std::string Router::route(std::string request_str) {
	std::string responce;

	// Request request(request_str);
	// request = request.substr(0, request.find("\n"));

	// if (request.getRequestedPage() == "GET / HTTP/1.1\r") {
	// 	responce = html.htmlFileToString("./html/index.html");
	// }
	return responce;
}
